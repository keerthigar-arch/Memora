# Deprecated

The single `Frontend` app was split into two projects:

- **`Frontend-Customer`** — public site (port 4200)
- **`Frontend-Admin`** — organizer site (port 4201)

See `FRONTEND_APPS.md` in the repository root. You can delete this `Frontend` folder if you no longer need it.
